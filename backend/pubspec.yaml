name: login_backend
description: Dart Backend mit JWT-Auth und SQLite (Serverpod-Architektur)

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.4
  shelf_router: ^1.1.4
  sqlite3: ^2.4.6
  dart_jsonwebtoken: ^2.14.2
  crypto: ^3.0.5

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
